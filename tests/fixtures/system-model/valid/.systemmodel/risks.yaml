risks:
  - id: risk.merge_bypass
    type: risk
    summary: Merge commands can bypass review if not centrally guarded.
    severity: high
    applies_to:
      capabilities: [capability.workspace_management]
      flows: [flow.approve_changes]
      files: [server/commands/*.ts]
    mitigation: Keep merge checks centralized.
