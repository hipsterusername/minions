version: 1
name: Minions system model
summary: Relevance-scoped context model for the Minions canvas agent-orchestration app. Flows are authored fine-grained and single-purpose so any one flow pulls in only its own small file footprint, never the whole model.
